<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <div class="navbar-brand <%= yield(:home) %>"><%= link_to "Home ", root_path %></div>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="<%= yield(:about) %>"><%= link_to "About Me ", about_me_path %><span class="sr-only">(current)</span></li>
        <li class="<%= yield(:video) %>"><%= link_to "Video Submission ", video_submission_path %></li>
        <li class="<%= yield(:contact) %>"><%= link_to "Contact Me ", contact_me_path %></li>
        <% if logged_in? %>
          <li class="dropdown <%= yield(:blog) %>">
            <a href="#" class='dropdown-toggle' data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Blog <span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'View All Post', blogs_path %></li>
              <li><%= link_to 'Create A Post', new_blog_path %></li>
            </ul>
          </li>
        <% else %>
          <li class="<%= yield(:blog) %>"><%= link_to "Blog", blogs_path %></li>
        <% end %>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if logged_in? %>
            <li><%= link_to "Log Out ", logout_path, method: :delete %></li>
        <% else %>
            <% unless current_page?(login_path) %>
              <li>
                <%= link_to 'Log In', login_path, { remote: true,
                                                    'data-toggle' => 'modal',
                                                    'data-target' => '#my-modal'} %>
              </li>
            <% end %>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>



